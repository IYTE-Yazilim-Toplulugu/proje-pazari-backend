global:
  resolve_timeout: 5m

route:
  receiver: default
  group_by:
    - alertname
    - service
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 3h

receivers:
  - name: default

# Production example (uncomment and adapt):
# receivers:
#   - name: slack
#     slack_configs:
#       - api_url: https://hooks.slack.com/services/YOUR/WEBHOOK/URL
#         channel: '#alerts'
#         send_resolved: true
