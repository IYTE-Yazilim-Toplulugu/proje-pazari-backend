package com.iyte_yazilim.proje_pazari.application.config;

import org.springframework.context.annotation.Configuration;

/**
 * Application layer configuration.
 *
 * <p>Note: Handlers, Validators, and Services are now annotated with @Service/@Component and are
 * automatically discovered by Spring's component scanning.
 */
@Configuration
public class ApplicationConfig {
    // All beans are now auto-configured via @Service, @Component annotations
}
