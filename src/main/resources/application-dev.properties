# Development Profile Configuration
# Uses MinIO for local blob storage (S3-compatible)

# Database - Local PostgreSQL via Docker
spring.datasource.url=jdbc:postgresql://localhost:5432/proje_pazari_db
spring.datasource.username=yazilim
spring.datasource.password=yazilim123

# JPA - Show SQL for debugging
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Storage - MinIO (local S3-compatible storage)
storage.provider=minio
minio.url=http://localhost:9002
minio.access-key=minioadmin
minio.secret-key=minioadmin123
minio.bucket-name=proje-pazari-files

# JWT - Development secret (DO NOT use in production)
jwt.secret=proje-pazari-dev-secret-key-2025-please-change-in-production-this-is-just-for-development
