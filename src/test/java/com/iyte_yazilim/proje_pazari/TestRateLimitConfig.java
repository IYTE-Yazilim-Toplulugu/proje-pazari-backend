package com.iyte_yazilim.proje_pazari;

import org.springframework.boot.test.context.TestConfiguration;

/**
 * Placeholder kept for import compatibility. Rate limiting is bypassed automatically when {@code
 * StringRedisTemplate} is mocked — the filter's null-checks skip all enforcement.
 */
@TestConfiguration
public class TestRateLimitConfig {}
