## Enable Gradle configuration cache to speed up builds
# See: https://docs.gradle.org/current/userguide/configuration_cache.html
org.gradle.configuration-cache=true

# Optional: increase maximum reported problems from configuration cache (adjust if needed)
# org.gradle.configuration-cache.max-problems=100
